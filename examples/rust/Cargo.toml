[dependencies]
reqwest = { version = "0.11", features = ["json", "blocking", "multipart", "gzip", "brotli", "deflate", "cookies", "stream"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
